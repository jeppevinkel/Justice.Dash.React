upstream backend {
    server ${BACKEND_HOST};
}

server {
    listen 80;

    location / {
      proxy_pass http://backend$request_uri;
    }

    location /web {
      root /usr/share/nginx/html;
      try_files $uri $uri/ /index.html;
    }
}